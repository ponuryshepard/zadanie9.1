var triangle1Area = getTriangleArea(10, 15);

var triangle2Area = getTriangleArea(48, 12);

var triangle3Area = getTriangleArea(2, 8);


var wynik = a*h/2;

function getTriangleArea(a, h) {
	
	if ( (a > 0) && (h > 0) ) {
	
	return wynik;
	} else if ( (a < 0) && (h < 0) ) {
	
	alert('Nieprawid³owe dane');
	
	} else ( (a == 0) && (h == 0) ) {
	
	alert('Nieprawid³owe dane');

	}

}